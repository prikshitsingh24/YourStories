module.exports = {
  content: [
    './index.html',           // Include the index.html if it's in the root
    './src/**/*.{js,jsx,ts,tsx}',  // Include all JS, JSX, TS, and TSX files in the src folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}